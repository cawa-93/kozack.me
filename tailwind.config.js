/** @type {import('tailwindcss').Config} */
export default {
    darkMode: 'media',
    theme: {
        extend: {
            colors: {
                brand: {
                    blue: {
                        DEFAULT: '#4cafdd'
                    }
                }
            }
        },
    },
    plugins: [],
};