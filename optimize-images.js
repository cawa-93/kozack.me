const fs = require('fs');
const path = require('path');

const preset = [
	{path: path.resolve('./src/GitHub.png'), width: 200 }
]


/**
 *
 * @param {{path: string, width: number}} preset
 */
function optimize(preset) {

}
